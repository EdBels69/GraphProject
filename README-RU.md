# 📋 Graph Analyser - Инструкция по запуску

## ✅ Статус проекта

- **Frontend**: ✅ Собран и готов к работе в папке `dist/`
- **Backend**: ⚠️ Требуется установка зависимостей
- **База данных**: ⚠️ Требуется Visual Studio для компиляции better-sqlite3

---

## 🚀 Быстрый старт (Frontend только)

### Открыть собранное приложение:

1. Откройте папку `dist/` в браузере
2. Или перейдите по ссылке: `file:///d:/Проекты/Проект 1/graph analyser/dist/index.html`

**Что работает:**
- ✅ Весь интерфейс пользователя
- ✅ Навигация между страницами
- ✅ Визуализация компонентов
- ✅ Все страницы приложения

**Что НЕ работает без backend:**
- ❌ Загрузка файлов
- ❌ Поиск в PubMed
- ❌ Сохранение в базу данных
- ❌ API функции

---

## 🔧 Полный запуск (Frontend + Backend)

### Вариант 1: Установка Visual Studio (рекомендуется)

**Шаг 1:** Установите Visual Studio Build Tools

1. Скачайте с: https://visualstudio.microsoft.com/visual-cpp-build-tools/
2. Запустите установщик
3. Выберите "Desktop development with C++"
4. Нажмите "Install"

**Шаг 2:** Установите зависимости

Откройте PowerShell или CMD и выполните:

```powershell
cd "d:\Проекты\Проект 1\graph analyser"
npm install --legacy-peer-deps
```

**Шаг 3:** Запустите сервер

```powershell
powershell -ExecutionPolicy Bypass -File start-server-v2.ps1
```

Или через npm:

```powershell
npm run dev:backend
```

**Шаг 4:** Откройте приложение

- Frontend: http://localhost:3000 (или откройте `dist/index.html`)
- Backend: http://localhost:3001

---

### Вариант 2: Использование готового Node.js (текущий)

**Текущая ситуация:**
- ✅ Node.js v24.12.0 установлен в `C:\Program Files\nodejs\node.exe`
- ❌ Нет Visual Studio Build Tools для компиляции native modules

**Что делать:**

1. **Откройте PowerShell** (Run → `powershell`)
2. **Перейдите в папку проекта:**
   ```powershell
   cd "d:\Проекты\Проект 1\graph analyser"
   ```

3. **Запустите сервер через PowerShell скрипт:**
   ```powershell
   powershell -ExecutionPolicy Bypass -File start-server-v2.ps1
   ```

**Если выдает ошибку better-sqlite3:**
- Установите Visual Studio Build Tools (см. Вариант 1)
- ИЛИ временно отключите БД в коде (для тестирования без БД)

---

### Вариант 3: Использование npm scripts

**Если npm найден в PATH:**

```powershell
cd "d:\Проекты\Проект 1\graph analyser"
npm run dev:backend
```

**Запуск всего приложения (frontend + backend):**

```powershell
npm run dev
```

---

## 📝 Скрипты для запуска

### start-server-v2.ps1
Находит Node.js и запускает backend сервер:
```powershell
powershell -ExecutionPolicy Bypass -File start-server-v2.ps1
```

### install-deps.ps1
Находит Node.js и устанавливает зависимости:
```powershell
powershell -ExecutionPolicy Bypass -File install-deps.ps1
```

---

## ⚠️ Частые проблемы

### 1. "npm: Имя не распознано"

**Решение:**
- Node.js не найден в PATH
- Используйте скрипты `start-server-v2.ps1` или `install-deps.ps1`
- Они автоматически находят Node.js

### 2. "node: Не является внутренней командой"

**Решение:**
- Используйте скрипты PowerShell, они находят Node.js автоматически

### 3. "better-sqlite3: Could not find Visual Studio"

**Решение:**
- Установите Visual Studio Build Tools
- Скачайте: https://visualstudio.microsoft.com/visual-cpp-build-tools/
- Выберите: "Desktop development with C++"

### 4. "ERR_MODULE_NOT_FOUND: Cannot find package 'multer'"

**Решение:**
- Установите зависимости:
  ```powershell
  powershell -ExecutionPolicy Bypass -File install-deps.ps1
  ```

---

## 🎯 Что работает сейчас

### Frontend (dist/)
- ✅ Главная страница
- ✅ Страница анализа
- ✅ Визуализация графа
- ✅ Анализ графа
- ✅ Статистика
- ✅ Пробелы в исследованиях
- ✅ Экспорт данных

### Backend (api/)
- ⚠️ Требуется Visual Studio для better-sqlite3
- ✅ Express сервер настроен
- ✅ CORS включен
- ✅ Upload middleware (multer) готов
- ⚠️ База данных требует компиляции native module

---

## 📦 Установленные пакеты

### Dependencies
- react, react-dom, react-router-dom
- zustand, axios, express, cors, dotenv
- uuid, @supabase/supabase-js, sonner
- multer, mammoth, pdf-parse
- better-sqlite3 (требует VS Build Tools)

### DevDependencies
- typescript, vite, eslint
- @types/* packages
- vitest, jsdom
- tailwindcss, postcss

---

## 🚀 Быстрый тест без БД

Если хотите протестировать интерфейс без backend:

1. Откройте `dist/index.html` в браузере
2. Проверьте все страницы и компоненты
3. Посмотрите консоль браузера (F12) на наличие ошибок

---

## 📞 Поддержка

Если возникнут проблемы:

1. Убедитесь, что Node.js установлен: `node --version`
2. Установите зависимости: `npm install --legacy-peer-deps`
3. Установите Visual Studio Build Tools для native modules
4. Используйте PowerShell скрипты для запуска

---

**Проект готов!** 🎉

Frontend работает без backend. Для полной функциональности установите Visual Studio Build Tools и запустите backend.
