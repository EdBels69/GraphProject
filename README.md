# Graph Analyser - Scientific Article Analysis Application

Приложение для анализа научных статей, извлечения ссылок, литературных обзоров, поиска пробелов в исследованиях и визуализации биохимических взаимодействий.

## 📋 Оглавление

- [Установка](#установка)
- [Запуск приложения](#запуск-приложения)
- [Структура проекта](#структура-проекта)
- [Технологический стек](#технологический-стек)
- [API Endpoints](#api-endpoints)
- [Новые функции](#новые-функции)
- [Разработка](#разработка)
- [Документация](#документация)
- [Changelog](#changelog)

---

## 🚀 Установка

### Требования

- Node.js 18+
- npm 9+
- TypeScript 5+
- Windows/macOS/Linux

### Шаги установки

```bash
# 1. Клонировать репозиторий
git clone https://github.com/YOUR_USERNAME/graph-analyser.git
cd graph-analyser

# 2. Установить зависимости
npm install --legacy-peer-deps

# 3. Запустить приложение
npm run dev
```

---

## ▶️ Запуск приложения

### Режим разработки

```bash
npm run dev
```

Приложение запустится на:

- Frontend: <http://localhost:3000>
- Backend: <http://localhost:3001>

### Режим продакшена

```bash
# Сборка
npm run build

# Предпросмотр
npm run preview
```

---

## 📁 Структура проекта

```
graph-analyser/
├── src/
│   ├── components/          # Переиспользуемые компоненты
│   │   ├── FileUploader.tsx    # Загрузка файлов (drag & drop)
│   │   ├── GraphViewer.tsx     # Визуализатор графов
│   │   ├── InputValidation.tsx  # Валидация ввода
│   │   ├── ErrorBoundary.tsx   # Обработчик ошибок
│   │   ├── MetricsCard.tsx      # Карточка метрик
│   │   ├── StatisticsTable.tsx  # Таблица статистики
│   │   ├── GraphFilters.tsx     # Фильтры графа
│   │   ├── ExportControls.tsx   # Управление экспортом
│   │   ├── ArticleCard.tsx      # Карточка статьи
│   │   └── ResearchGapCard.tsx  # Карточка пробела
│   ├── hooks/               # React хуки
│   │   ├── useApi.ts          # API хук с retry
│   │   └── useGraph.ts       # Graph хук
│   ├── pages/               # Страницы приложения
│   │   ├── HomePage.tsx             # Домашняя страница
│   │   ├── AnalysisPage.tsx          # Анализ
│   │   ├── GraphAnalysisPage.tsx      # Анализ графов
│   │   ├── GraphVisualizationPage.tsx  # Визуализация графов
│   │   ├── StatisticsPage.tsx        # Статистика
│   │   ├── ResearchGapsPage.tsx      # Пробелы в исследованиях
│   │   └── ExportDataPage.tsx       # Экспорт данных
│   ├── store/               # State management
│   │   └── useAppStore.ts    # Zustand store
│   ├── utils/               # Утилиты
│   │   ├── fileParser.ts     # Парсер файлов (CSV, JSON, BibTeX)
│   │   ├── validators.ts      # Валидаторы
│   │   └── retryHandler.ts   # Обработчик повторений
│   ├── core/                # Ядро системы
│   │   ├── Database.ts       # База данных (SQLite)
│   │   ├── Logger.ts         # Логирование
│   │   ├── SessionManager.ts # Управление сессиями
│   │   ├── CacheManager.ts   # Кэширование
│   │   └── ErrorHandler.ts  # Обработка ошибок
│   ├── App.tsx              # Главный компонент
│   ├── main.tsx             # Точка входа
│   └── index.css            # Глобальные стили
├── api/                    # Бэкенд
│   ├── server.ts             # Express сервер
│   ├── routes/              # API роуты
│   │   ├── articles.ts       # API статей
│   │   ├── graphs.ts        # API графов
│   │   ├── pubmed.ts        # PubMed API
│   │   ├── analysis.ts      # API анализа
│   │   ├── gaps.ts         # API пробелов
│   │   ├── statistics.ts    # API статистики
│   │   └── export.ts       # API экспорта
│   ├── services/            # Сервисы
│   │   └── pubmedService.ts # Сервис PubMed
│   └── controllers/         # Контроллеры
├── shared/                 # Общие модули
│   ├── types.ts             # Типы TypeScript
│   ├── graphAlgorithms.ts   # Графовые алгоритмы
│   ├── graphStorage.ts      # Хранилище графов
│   └── graphAlgorithms.test.ts # Тесты алгоритмов
├── public/                 # Статические файлы
├── uploads/                # Загруженные файлы
├── index.html              # HTML точка входа
├── vite.config.ts          # Конфигурация Vite
├── vitest.config.ts        # Конфигурация Vitest
├── tsconfig.json           # Конфигурация TypeScript
├── package.json            # Зависимости и скрипты
├── .eslintrc.cjs          # ESLint конфигурация
├── .prettierrc            # Prettier конфигурация
├── README.md               # Документация
└── CHANGELOG.md            # История изменений
```

---

## 🛠 Технологический стек

### Frontend

| Технология | Версия | Назначение |
|-----------|-----------|-------------|
| React | 18.3.1 | UI библиотека |
| React Router DOM | 6.22.3 | Навигация |
| TypeScript | 5.x | Типизация |
| Vite | 6.1.0 | Build tool |
| Tailwind CSS | 3.4.21 | Стили |
| Zustand | 5.0.2 | State management |
| Axios | 1.7.9 | HTTP клиент |
| Sonner | 1.5.0 | Toast уведомления |
| Cytoscape.js | 3.30.3 | Визуализация графов |
| Testing Library | Latest | Тестирование компонентов |
| Vitest | 3.0.7 | Тестовый раннер |

### Backend

| Технология | Версия | Назначение |
|-----------|-----------|-------------|
| Express | 4.21.2 | Web сервер |
| TypeScript | 5.x | Типизация |
| Prisma | 5.22.0 | ORM & Database |
| Better-Sqlite3 | 11.8.1 | База данных (через Prisma) |
| Socket.IO | 4.8.3 | Real-time обновления |
| CORS | 2.8.5 | Cross-origin requests |
| Multer | Latest | Загрузка файлов |

### External APIs

| API | Назначение |
|-----|-----------|
| PubMed E-utilities | PubMed поиск и детали статей |
| Crossref | Метаданные и ссылки |
| ArXiv | Поиск препринтов |
| Unpaywall | Поиск открытых PDF |

---

## 🔌 API Endpoints

### Статьи

| Endpoint | Метод | Описание |
|----------|--------|----------|
| GET /articles | Получение списка статей |
| POST /articles | Создание статьи |
| GET /articles/:id | Получение статьи по ID |
| PUT /articles/:id | Обновление статьи |
| DELETE /articles/:id | Удаление статьи |

### Графы

| Endpoint | Метод | Описание |
|----------|--------|----------|
| GET /api/graphs | Получение всех графов |
| GET /api/graphs/:id | Получение графа по ID |
| POST /api/graphs | Создание нового графа |
| PUT /api/graphs/:id | Обновление графа |
| DELETE /api/graphs/:id | Удаление графа |
| POST /api/graphs/upload | **Загрузка файла (CSV, JSON, BibTeX)** |
| GET /api/graphs/:id/nodes | Получение узлов графа |
| POST /api/graphs/:id/nodes | Добавление узла |
| DELETE /api/graphs/:graphId/nodes/:nodeId | Удаление узла |
| GET /api/graphs/:id/edges | Получение ребер графа |
| POST /api/graphs/:id/edges | Добавление ребра |
| DELETE /api/graphs/:graphId/edges/:edgeId | Удаление ребра |
| GET /api/graphs/:id/shortest-path | Поиск кратчайшего пути |
| GET /api/graphs/:id/all-shortest-paths | Все кратчайшие пути |
| GET /api/graphs/:id/centrality | Расчет центральности |
| GET /api/graphs/:id/connectivity | Проверка связности |
| GET /api/graphs/:id/statistics | Статистика графа |
| POST /api/graphs/:id/validate | Валидация графа |

### PubMed

| Endpoint | Метод | Описание |
|----------|--------|----------|
| GET /pubmed/search | **Поиск статей в PubMed** |
| POST /pubmed/network | **Создание сети цитирований** |
| GET /pubmed/article/:pmid | **Получение деталей статьи** |

### Анализ

| Endpoint | Метод | Описание |
|----------|--------|----------|
| POST /api/analysis/start | Запуск анализа |
| POST /api/analysis/pause | Пауза анализа |
| POST /api/analysis/resume | Продолжение анализа |
| GET /api/analysis/status | Статус анализа |

### Экспорт

| Endpoint | Метод | Описание |
|----------|--------|----------|
| GET /api/export/graphs | Экспорт графов (JSON, CSV, GraphML) |
| GET /api/export/articles | Экспорт статей (JSON, CSV, BibTeX) |

---

## ✨ Новые функции

### 1. Загрузка файлов

**Поддерживаемые форматы:**

- **CSV** - файлы с разделителями запятыми
- **JSON** - массивы объектов статей
- **BibTeX** - библиографические записи

**Формат CSV:**

```csv
id,title,authors,year,abstract,keywords,citations
article-1,"Test Article","Author 1; Author 2",2023,"Abstract","keyword1;keyword2","article-2"
```

**Формат JSON:**

```json
{
  "articles": [
    {
      "id": "article-1",
      "title": "Test Article",
      "authors": ["Author 1"],
      "year": 2023,
      "abstract": "Abstract text",
      "keywords": ["keyword1"],
      "citations": ["article-2"]
    }
  ]
}
```

**Формат BibTeX:**

```bibtex
@article{article1,
  title = {Test Article},
  author = {Author 1 and Author 2},
  year = {2023},
  abstract = {Abstract text},
  keywords = {keyword1, keyword2}
}
```

### 2. Интеграция с PubMed

**Поиск статей:**

```bash
GET /pubmed/search?q=cancer&maxResults=20&year=2023
```

**Создание сети цитирований:**

```bash
POST /pubmed/network
{
  "query": "cancer immunotherapy",
  "maxArticles": 20,
  "maxCitations": 10,
  "depth": 1
}
```

**Получение деталей статьи:**

```bash
GET /pubmed/article/12345678
```

### 3. Кэширование результатов анализа

- Автоматическое кэширование результатов вычислений
- Кратчайшие пути кэшируются на 10 минут
- Метрики центральности кэшируются на 15 минут
- LRU eviction для оптимального использования памяти

### 4. CRUD операции с графами в БД

- Полное сохранение графов в SQLite
- Автоматическая загрузка при старте сервера
- Индексы для оптимизации запросов
- Поддержка UPSERT (обновление при существующем ID)

### 5. Валидация данных

- Валидация на клиенте (React компоненты)
- Валидация на сервере (API routes)
- Поддержка кастомных ошибок
- Подробные сообщения об ошибках

### 6. Расширенный поиск литературы

- **ArXiv & BioRxiv**: Поиск препринтов (в дополнение к PubMed/Crossref)
- **Умное скачивание**: Retry logic с экспоненциальной задержкой для PDF
- **Resilience**: Обработка 429/5xx ошибок внешних API

### 7. Real-time Обновления

- **Socket.IO**: Мгновенное обновление прогресса (нет polling)
- **Optimistic UI**: Мгновенная реакция интерфейса на действия пользователя
- **Предотвращение потери данных**: Блокировка закрытия вкладки при несохраненных изменениях

---

## 💻 Разработка

### Добавление новой страницы

1. Создать файл в `src/pages/NewPage.tsx`:

```tsx
import { useState } from 'react'

export default function NewPage() {
  const [data, setData] = useState(null)
  
  return (
    <div className="container">
      <h1>Новая страница</h1>
    </div>
  )
}
```

1. Добавить роут в `src/App.tsx`:

```tsx
import NewPage from '@/pages/NewPage'

<Route path="/new-page" element={<NewPage />} />
```

### Использование FileUploader

```tsx
import { FileUploader } from '@/components/FileUploader'
import { toast } from 'sonner'

function UploadPage() {
  const handleFileUpload = async (file: File) => {
    try {
      const formData = new FormData()
      formData.append('file', file)
      
      const response = await fetch('/api/graphs/upload', {
        method: 'POST',
        body: formData
      })
      
      const result = await response.json()
      toast.success('Файл загружен!')
    } catch (error) {
      toast.error('Ошибка загрузки')
    }
  }

  return (
    <FileUploader
      onFileUpload={handleFileUpload}
      isLoading={false}
      error={undefined}
    />
  )
}
```

### Использование валидаторов

```typescript
import { validateArticle, validateGraph } from '@/utils/validators'

const article = {
  id: 'article-1',
  title: 'Test Article',
  authors: ['Author 1'],
  year: 2023
}

const validation = validateArticle(article)
if (!validation.valid) {
  console.error('Ошибки:', validation.errors)
}
```

### Использование кэша

```typescript
import { cacheManager } from '@/core/CacheManager'

// Сохранение в кэш
cacheManager.set('key', { data: 'value' }, 60000) // 60 секунд

// Получение из кэша
const value = cacheManager.get('key')

// Проверка наличия
if (cacheManager.has('key')) {
  console.log('Ключ существует в кэше')
}

// Удаление из кэша
cacheManager.delete('key')

// Очистка по шаблону
cacheManager.clearPattern('^graph:')
```

---

## 📚 Документация

### Команды

```bash
# Установка зависимостей
npm install --legacy-peer-deps

# Запуск разработки
npm run dev

# Сборка
npm run build

# Запуск продакшена
npm run preview

# Проверка типов
npm run typecheck

# Линтинг
npm run lint

# Запуск тестов
npm run test

# Покрытие тестами
npm run test:coverage
```

### Переменные окружения

Создать файл `.env` в корне проекта:

```env
# API URL
API_BASE_URL=http://localhost:3001
PORT=3001

# Database
DATABASE_URL="file:./dev.db"

# LLM Configuration (Optional)
LLM_PROVIDER=local # local (Ollama) | openai | anthropic
OLLAMA_HOST=http://localhost:11434
# OPENAI_API_KEY=sk-...
# ANTHROPIC_API_KEY=sk-ant-...

# External APIs (Optional)
PUBMED_API_KEY=your_key_here
```

---

## 🐛 Устранение неполадок

### Ошибки при установке зависимостей

```bash
# Используйте флаг --legacy-peer-deps
npm install --legacy-peer-deps
```

### Проблемы с загрузкой файлов

1. Проверьте размер файла (максимум 10 МБ)
2. Убедитесь, что формат поддерживается (CSV, JSON, BibTeX)
3. Проверьте логи сервера на наличие ошибок парсинга

### Ошибки PubMed API

1. Убедитесь, что подключен интернет
2. Проверьте API ключ в переменных окружения
3. Соблюдайте лимиты запросов PubMed (3 запроса в секунду без ключа)

### Проблемы с кэшем

1. Проверьте логи CacheManager
2. Убедитесь, что TTL настроен корректно
3. Проверьте maxEntries лимит

---

## 📄 Changelog

См. [CHANGELOG.md](./CHANGELOG.md) для истории изменений.

---

## 📄 Лицензия

MIT License - свободно для использования в коммерческих и некоммерческих проектах.

---

## 👥 Автор

Graph Analyser Team

---

## 🔗 Ссылки

- [Vite Documentation](https://vitejs.dev/)
- [React Documentation](https://react.dev/)
- [TypeScript Documentation](https://www.typescriptlang.org/docs/)
- [Tailwind CSS Documentation](https://tailwindcss.com/docs)
- [PubMed E-utilities API](https://www.ncbi.nlm.nih.gov/books/NBK25501/)

---

## 🤝 Контрибьюции

Вклад в проект приветствуется! Пожалуйста:

1. Форкните репозиторий
2. Создайте ветку для вашего фичи: `git checkout -b feature/my-feature`
3. Внесите изменения
4. Добавьте тесты
5. Убедитесь, что тесты проходят: `npm test`
6. Отправьте pull request

---

## 📞 Поддержка

Если вы нашли баг или хотите задать вопрос:

1. Откройте [GitHub Issues](https://github.com/YOUR_USERNAME/graph-analyser/issues)
2. Опишите проблему подробно
3. Приложите скриншоты
4. Укажите окружение (OS, браузер)
5. Добавьте логи из консоли браузера и сервера
