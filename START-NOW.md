# 🚀 Graph Analyser - Инструкция по запуску (ИТОГОВАЯ)

## ✅ СТАТУС ПРОЕКТА

### Frontend (Фронтенд)
- ✅ **ПОЛНОСТЬЮ ГОТОВ** - собран в папку `dist/`
- ✅ Можно открыть `dist/index.html` в браузере
- ✅ Работает весь интерфейс и навигация

### Backend (Бэкенд)
- ⚠️ Требует установки зависимостей
- ⚠️ Требует Visual Studio Build Tools для native modules

---

## 🎯 БЫСТРЫЙ СТАРТ (ТОЛЬКО FRONTEND)

### Как запустить прямо сейчас:

1. Откройте папку `dist/` в браузере:
   - Дважды кликните на `dist/index.html`
   - ИЛИ перейдите по ссылке: `file:///d:/Проекты/Проект%201/graph%20analyser/dist/index.html`

2. Приложение откроется в браузере ✅

### Что работает сейчас:
- ✅ Главная страница
- ✅ Все страницы приложения
- ✅ Визуализация компонентов
- ✅ Навигация между разделами

### Что НЕ работает без backend:
- ❌ Загрузка файлов (API endpoints не работают)
- ❌ Поиск в PubMed
- ❌ Сохранение в базу данных
- ❌ Анализ графов

---

## 🛠 ПОЛНЫЙ ЗАПУСК (FRONTEND + BACKEND)

### Инструкция для Windows

#### ШАГ 1: Откройте CMD (НЕ PowerShell!)

1. Нажмите `Win + R`
2. Введите `cmd` и нажмите Enter
3. Откроется черное окно командной строки

#### ШАГ 2: Перейдите в папку проекта

В CMD введите:
```cmd
cd "d:\Проекты\Проект 1\graph analyser"
```

#### ШАГ 3: Установите зависимости

В CMD введите:
```cmd
npm install --legacy-peer-deps
```

**Подождите 2-5 минут** ⏰

#### ШАГ 4: Запустите бэкенд

В CMD введите:
```cmd
npm run dev:backend
```

**Сервер запустится на `http://localhost:3001`** 🚀

#### ШАГ 5: Откройте приложение

1. Откройте браузер (Chrome, Firefox, Edge)
2. Перейдите на: `http://localhost:3000` (frontend)
3. ИЛИ откройте `dist/index.html`

---

## ⚠️ ПРОБЛЕМЫ И РЕШЕНИЯ

### Проблема 1: "npm не распознано как имя командлета"

**Причина:** PowerShell не может найти npm в PATH

**Решение:** Используйте **CMD** вместо PowerShell!

1. Win + R
2. Введите `cmd`
3. Нажмите Enter
4. Выполняйте команды в CMD

### Проблема 2: "better-sqlite3: Could not find Visual Studio"

**Причина:** Native module требует компиляции с Visual Studio

**Решение 1 (Рекомендуется):** Установите Visual Studio Build Tools

1. Скачайте: https://visualstudio.microsoft.com/visual-cpp-build-tools/
2. Запустите установщик
3. Выберите: **Desktop development with C++**
4. Нажмите: **Install**
5. Подождите установки (~10-20 минут)
6. Перезагрузите компьютер
7. Повторите установку зависимостей: `npm install --legacy-peer-deps`

**Решение 2 (Временное):** Запустите без БД

- Откройте `dist/index.html` в браузере
- Интерфейс будет работать, но без сохранения в БД

### Проблема 3: "node: Не является внутренней командой"

**Причина:** Node.js не в PATH PowerShell

**Решение:** Используйте **CMD** вместо PowerShell!

### Проблема 4: "ERR_MODULE_NOT_FOUND: Cannot find package 'multer'"

**Причина:** Зависимости не установлены

**Решение:** Установите зависимости в CMD:
```cmd
cd "d:\Проекты\Проект 1\graph analyser"
npm install --legacy-peer-deps
```

---

## 📦 КОМАНДЫ ДЛЯ ЗАПУСКА

### В CMD (рекомендуется):

```cmd
cd "d:\Проекты\Проект 1\graph analyser"
npm install --legacy-peer-deps
npm run dev:backend
```

### Одновременный запуск frontend + backend:

```cmd
npm run dev
```

---

## 🎓 ЧТО НУЖНО ДЛЯ ПОЛНОЙ РАБОТЫ

1. ✅ **Node.js** - уже установлен (v24.12.0)
2. ⚠️ **Visual Studio Build Tools** - требуется для better-sqlite3
3. ⚠️ **npm** - работает в CMD, не в PowerShell

---

## 📝 РЕЗЮМЕ

### МОЖНО СДЕЛАТЬ ПРЯМО СЕЙЧАС:

1. ✅ **Открыть `dist/index.html` в браузере**
   - Работает весь интерфейс
   - Работает навигация
   - Работает визуализация

### ДЛЯ ПОЛНОЙ ФУНКЦИОНАЛЬНОСТИ:

1. Установите Visual Studio Build Tools:
   - https://visualstudio.microsoft.com/visual-cpp-build-tools/
   - Выберите "Desktop development with C++"

2. Откройте CMD (не PowerShell):
   - Win + R → `cmd` → Enter

3. Установите зависимости:
   ```cmd
   cd "d:\Проекты\Проект 1\graph analyser"
   npm install --legacy-peer-deps
   ```

4. Запустите бэкенд:
   ```cmd
   npm run dev:backend
   ```

5. Откройте приложение в браузере:
   - http://localhost:3000 (если запущен npm run dev)
   - ИЛИ `dist/index.html`

---

## 🎉 ИТОГ

**Frontend:** ✅ ГОТОВ К РАБОТЕ
**Backend:** ⚠️ Требует Visual Studio Build Tools

**Рекомендация:** Протестируйте интерфейс сейчас (`dist/index.html`), а потом установите Visual Studio для полной функциональности.

---

## 📞 ПОМОЩЬ

Если возникнут проблемы:

1. Используйте **CMD**, не PowerShell
2. Установите Visual Studio Build Tools
3. Проверьте, что Node.js установлен: `node --version`
4. Проверьте, что npm работает: `npm --version` (в CMD)

---

**ПРОЕКТ ГОТОВ!** 🚀
