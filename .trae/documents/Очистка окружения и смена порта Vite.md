# План очистки окружения и обхода ошибки /api/webviewClick

## 🔍 Текущий статус проверки:

| Компонент | Статус | Детали |
|------------|----------|---------|
| index.html | ✅ Чист | Только 1 script (main.tsx) |
| main.tsx | ✅ Чист | Нет serviceWorker кода |
| vite.config.ts | ✅ Ок | Порт 3000 |
| src/**/*.tsx | ✅ Чист | Нет /api/webviewClick |

---

## 🚨 ПРИЧИНА БЛОКИРОВКИ:

Ошибка `/api/webviewClick` исходит **НЕ ИЗ ПРОЕКТА**, а от:
- ✅ Закешированного Service Worker
- ✅ Встроенных скриптов Trae IDE
- ✅ Лишних тегов в index.html

**Вероятные источники:**
1. Кеш браузера (наиболее вероятно)
2. Расширения браузера (например, для VS Code)
3. Build кеш Vite (`node_modules/.vite`)

---

## 🛠️ ШАГИ ИСПРАВЛЕНИЯ:

### 1. Остановка Vite
Остановить текущий процесс (terminal 2):

```powershell
# Нажмите Ctrl+C в терминале 2
```

### 2. Очистка кеша

```powershell
# В новом терминале (terminal 5):
rm -rf node_modules/.vite dist
```

### 3. Смена порта Vite

Открыть `vite.config.ts` и изменить порт:

```typescript
// Изменить с 3000 на 5173:
server: {
  port: 5173,  // БЫЛО: 3000
  // ... остальной код
}
```

### 4. Перезапуск сервера

```powershell
npm run dev
```

### 5. Очистка кеша браузера

Открыть DevTools (F12) → Application → Clear storage → Перезагрузить (Ctrl+F5)

---

## 📋 ОЖИДАЕМЫЙ РЕЗУЛЬТАТ:

| Проверка | Ожидаемое значение |
|-----------|---------------------|
| Приложение запускается на новом порте (5173) | ✅ http://localhost:5173 |
| Белый экран исчезает | ✅ UI загружается |
| Кнопки работают | ✅ onClick реагирует |
| Ошибка `/api/webviewClick` исчезает | ✅ Нет timeout |
| Консоль браузера без ошибок | ✅ Чистая |

---

## 🎯 ПРИНЦИП РЕШЕНИЯ:

Смена порта разорвет подключение к IDE API, что предотвратит блокирующий запрос `/api/webviewClick`.