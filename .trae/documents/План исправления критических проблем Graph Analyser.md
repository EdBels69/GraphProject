# План исправления критических проблем проекта Graph Analyser

---

## 🔍 Результаты аудита

### ✅ Что работает корректно:
1. **package.json** - корректный, все зависимости прописаны
2. **vite.config.ts** - конфигурация правильная, порт 3000, проксирование на 3001
3. **api/server.ts** - структура сервера корректная, импорты маршрутов правильные
4. **src/main.tsx** - точка входа корректная, логирование загружи реализовано
5. **src/App.tsx** - маршрутизация React правильная, все страницы подключены
6. **src/components/AppHealthCheck.tsx** - система проверки загружи реализована
7. **API маршруты** - структура маршрутов правильная
8. **Типы данных** - интерфейсы определены корректно

### ❌ Критические проблемы:

#### 1. **ПАКЕТ better-sqlite3 НЕ УСТАНОВЛЕН** (КРИТИЧНО)
- **Проблема**: В [api/server.ts](api/server.ts#L70) вызывается `databaseManager.initialize()`, который импортирует `better-sqlite3`
- **Ошибка**: `ERR_MODULE_NOT_FOUND: Cannot find package 'better-sqlite3'`
- **Причина**: `node_modules/` отсутствует в проекте
- **Влияние**: Бэкенд не запускается, база данных не инициализируется

#### 2. **Архитектурная проблема - данные хранятся в памяти** (КРИТИЧНО)
- **Проблема**: В [api/routes/articles.ts](api/routes/articles.ts#L15) используется `const articles: Article[] = []`
- **Причина**: Не используется база данных, данные теряются при перезапуске сервера
- **Влияние**: Потеря всех загруженных статей при каждом перезапуске
- **Несогласованность**: `api/server.ts` инициализирует `databaseManager`, но маршруты его не используют

#### 3. **Отсутствие файлов окружения**
- **Проблема**: Файл `.env` отсутствует (есть только `.env.example`)
- **Причина**: Может использоваться значения по умолчанию, что нежелательно для продакшена

---

## 🛠️ План исправления

### Этап 1: Установка зависимостей (КРИТИЧНО)

**Цель**: Установить все пакеты из package.json

**Действия**:
1. Установить все production зависимости
   ```bash
   npm install
   ```
   
2. Если `better-sqlite3` не устанавливается на Windows:
   - Попробовать альтернативный вариант установки
   - Или создать fallback на `sqlite3`

**Проверка**: После установки проверить наличие `node_modules/better-sqlite3/`

---

### Этап 2: Обновление маршрутов для использования базы данных (КРИТИЧНО)

**Цель**: Интегрировать базу данных вместо хранения в памяти

**Файлы для изменения**:
1. `api/routes/articles.ts` - обновить для использования `databaseManager`
2. Проверить другие маршруты на аналогичную проблему

**Действия**:
1. Заменить хранение в памяти на использование `databaseManager`:
   - `POST /articles` → `databaseManager.createArticle()`
   - `GET /articles` → `databaseManager.getArticles()`
   - `GET /articles/:id` → `databaseManager.getArticle()`
   - `PUT /articles/:id` → `databaseManager.updateArticle()`
   - `DELETE /articles/:id` → `databaseManager.deleteArticle()`

2. Обновить типы данных для соответствия базе данных

**Проверка**: После перезапуска данные должны сохраняться в `data/app.db`

---

### Этап 3: Создание файла окружения

**Цель**: Создать `.env` файл с правильными настройками

**Действия**:
1. Скопировать `.env.example` в `.env`
2. Настроить переменные (если необходимо)

**Проверка**: Файл `.env` должен существовать

---

### Этап 4: Проверка и исправление импортов

**Цель**: Обеспечить корректность путей импорта

**Действия**:
1. Проверить все импорты в API файлах
2. Убедиться, что пути к модулям корректны
3. Убедиться, что импорты типов TypeScript корректны

**Проверка**: Команда `npm run typecheck` должна выполняться без ошибок

---

### Этап 5: Тестирование после каждого изменения

**После каждого этапа**:
1. Запустить `npm run dev`
2. Проверить логи консоли:
   - Нет ошибок `ERR_MODULE_NOT_FOUND`
   - База данных инициализируется успешно
   - Сервер запускается на порту 3001
3. Проверить в браузере:
   - `http://localhost:3000` - главная страница загружается
   - `http://localhost:3001/health` - health check возвращает `{"status": "ok"}`
   - `http://localhost:3000/admin` - админская панель доступна
4. Проверить, что данные сохраняются в базе:
   - Загрузить статью через API
   - Перезапустить сервер
   - Данные должны сохраниться

---

## 📋 Порядок выполнения:

1. **Шаг 1**: Установить зависимости (`npm install`)
2. **Шаг 2**: Обновить `api/routes/articles.ts` для использования базы данных
3. **Шаг 3**: Создать `.env` файл
4. **Шаг 4**: Запустить `npm run dev`
5. **Шаг 5**: Проверить работоспособность всех endpoints
6. **Шаг 6**: Проверить сохранение данных в базе

---

## ⚠️ Фолбэк-план (если better-sqlite3 не установится):

Если `better-sqlite3` не устанавливается на Windows:

**Вариант A**: Использовать `sqlite3` вместо `better-sqlite3`
1. Обновить `package.json`:
   ```json
   "dependencies": {
     "sqlite3": "^5.1.7",
     "better-sqlite3": "^9.4.3"
   }
   ```
2. Обновить `src/core/Database.ts`:
   ```typescript
   import sqlite3 from 'sqlite3';
   ```
3. Переписать методы для использования `sqlite3` API (callback-based вместо sync)

**Вариант B**: Пропустить базу данных временно
1. Создать простое хранилище в памяти
2. Заменить все вызовы `databaseManager` на простые структуры данных
3. Функционал будет работать, но данные не будут сохраняться

---

## 🎯 Ожидаемый результат:

После выполнения всех этапов:
- ✅ Все зависимости установлены
- ✅ База данных инициализируется без ошибок
- ✅ Сервер запускается на порту 3001
- ✅ Frontend работает на порту 3000
- ✅ Данные сохраняются в `data/app.db`
- ✅ После перезапуска данные не теряются
- ✅ Все страницы приложения доступны
- ✅ Админская панель работает
- ✅ Консоль управления работает (`npm run cli`)

---

## 📝 Документация изменений:

Все изменения будут документированы в отдельном файле:
- `docs/FIXES_SUMMARY.md` - итоговый отчёт об исправлениях
- Каждый шаг будет сопровождаться комментариями в коде

---

## ⚡ Приоритеты:

**КРИТИЧНО** (должно быть исправлено немедленно):
1. Установка `better-sqlite3` или альтернативы
2. Обновление маршрутов для использования базы данных

**ВАЖНО**:
3. Создание `.env` файла

**ЖЕЛАТЕЛЬНО**:
4. Оптимизация запросов к базе данных
5. Добавление индексов
6. Тестирование производительности