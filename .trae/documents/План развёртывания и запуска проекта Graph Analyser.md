# План развёртывания и запуска проекта Graph Analyser

## 📋 Анализ проекта

**Текущее состояние:**
- ✅ Полная структура проекта создана
- ✅ Все модули ядра реализованы (Logger, ErrorHandler, SessionManager, Database)
- ✅ Веб-интерфейс администратора готов
- ✅ CLI консоль управления готов
- ✅ Unit-тесты (104 теста) и интеграционные тесты (12 тестов)
- ✅ Документация создана

**Требования к системе:**
- Node.js 18+
- npm 9+
- TypeScript 5+
- Windows/macOS/Linux

---

## 🎯 План действий

### 1. Установка зависимостей
```bash
npm install
```
- Установит все production и dev зависимости
- Включая: better-sqlite3, express, react, typescript и т.д.

### 2. Создание файла окружения (.env)
- Скопировать `.env.example` в `.env`
- Настроить переменные окружения (для разработки можно использовать значения по умолчанию)

### 3. Проверка структуры директорий
- Убедиться, что директория `data/` существует
- Создать её при необходимости

### 4. Запуск проекта в режиме разработки
```bash
npm run dev
```
Эта команда запустит:
- Frontend (Vite) на порту **3000**
- Backend (Express) на порту **3001**

### 5. Проверка работоспособности
- Открыть `http://localhost:3000` в браузере
- Проверить главную страницу
- Открыть админскую панель: `http://localhost:3000/admin`
- Проверить, что backend отвечает на `http://localhost:3001/health`

### 6. Запуск тестов (опционально)
```bash
npm test
```

### 7. Запуск CLI консоли (опционально)
```bash
npm run cli
```

---

## ✅ Проверки после запуска

1. **Frontend доступен:** `http://localhost:3000`
2. **Backend доступен:** `http://localhost:3001/health`
3. **Админская панель:** `http://localhost:3000/admin`
4. **База данных создана:** файл `data/app.db`
5. **Нет ошибок в терминалах**
6. **Все компоненты отображаются корректно**

---

## 📦 Что будет установлено

**Основные зависимости:**
- React 18.3.1, React DOM 18.3.1
- React Router DOM 6.22.3
- Express 4.19.2
- TypeScript 5.5.4
- Vite 5.3.1
- Tailwind CSS 3.4.12
- Zustand 4.5.2
- better-sqlite3 9.4.3

**Инструменты разработки:**
- tsx 4.16.2 (TypeScript executor)
- concurrently 8.2.2 (параллельный запуск)
- vitest 1.6.0 (тестирование)
- eslint 9.8.0 (линтинг)

---

## ⚠️ Возможные проблемы и решения

**Проблема: Порт уже занят**
- Решение: Остановить процессы или изменить порт в `vite.config.ts` и `api/server.ts`

**Проблема: Ошибка компиляции TypeScript**
- Решение: Запустить `npm run typecheck` для диагностики

**Проблема: Ошибка подключения к базе данных**
- Решение: Проверить права на директорию `data/`

**Проблема: better-sqlite3 не компилируется**
- Решение: Убедиться, что установлены build tools (node-gyp)

---

## 📊 Ожидаемый результат

После выполнения плана:
- ✅ Все зависимости установлены
- ✅ Frontend работает на порту 3000
- ✅ Backend работает на порту 3001
- ✅ База данных SQLite инициализирована
- ✅ Админская панель доступна
- ✅ Консоль управления (CLI) работает
- ✅ Тесты проходят успешно