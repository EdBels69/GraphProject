# Задача: Интеграция Supabase для сохранения данных

## 1. Описание проблемы

**Статус:** В текущем состоянии приложение использует mock данные в backend API маршрутах.

**Затронутые компоненты и модули:**
- `api/server.ts` - главный Express сервер
- `api/routes/articles.ts` - маршруты для статей
- `api/routes/analysis.ts` - маршруты для анализа
- `api/routes/graph.ts` - маршруты для графов
- `api/routes/gaps.ts` - маршруты для research gaps
- `api/routes/statistics.ts` - маршруты для статистики
- `api/routes/export.ts` - маршруты для экспорта
- `supabase/client.ts` - конфигурация Supabase клиента (создан, но не используется)
- Все страницы в `src/pages/` - компоненты, которые будут запрашивать данные

**Проблема:** 
Backend API возвращает статические mock данные вместо запросов к реальной базе данных Supabase. Это делает приложение непригодным для реального использования, так как:
- Загруженные статьи не сохраняются
- Результаты анализа не хранятся
- Данные теряются при перезапуске сервера
- Нет возможности работы с большими объемами данных

## 2. Ожидаемое поведение

**Основной функционал:**
1. Сохранение загруженных статей в базу данных
2. Хранение результатов анализа статей (сущности, взаимодействия)
3. Сохранение данных о графах и research gaps
4. Хранение статистики и метрик
5. Реальный экспорт данных из базы в различных форматах
6. Аутентификация пользователей через Supabase Auth

**Сценарии использования:**

### Сценарий 1: Загрузка статей
- Пользователь загружает PDF файлы статей
- Статьи парсятся и извлекается информация
- Данные сохраняются в таблицу `articles` в Supabase
- Возвращается ID сохраненной статьи для отслеживания

### Сценарий 2: Анализ статей
- Пользователь запускает анализ для загруженных статей
- Анализ извлекает сущности (белки, гены, метаболиты)
- Сущности сохраняются в таблицу `entities`
- Взаимодействия сохраняются в таблицу `interactions`
- Результаты анализа сохраняются в таблицу `analysis_results`
- Прогресс отслеживается через таблицу `analysis_progress`

### Сценарий 3: Графовая визуализация
- Фронтенд запрашивает узлы и ребра графа
- Backend делает запрос к таблицам `entities` и `interactions`
- Данные преобразуются в формат графа
- Кэширование результатов для быстрой загрузки

### Сценарий 4: Статистика
- Агрегация данных из всех таблиц
- Вычисление метрик (количество статей, сущностей, взаимодействий)
- Расчет временных метрик (распределение по времени)
- Определение топовых сущностей по связям

### Сценарий 5: Экспорт данных
- Выбор данных для экспорта (статьи, сущности, граф)
- Формирование данных из Supabase
- Преобразование в выбранный формат (JSON, CSV, XLSX, GEXF)
- Генерация файла для скачивания

### Сценарий 6: Аутентификация
- Вход через email/password
- Регистрация новых пользователей
- Разделение данных между пользователями (user_id в таблицах)
- Безопасный доступ к данным через RLS policies

## 3. Требования к реализации

### Технические спецификации

**База данных Supabase:**
- Использовать существующую схему из `supabase/client.ts`
- Таблицы: `articles`, `entities`, `interactions`, `analysis_results`, `analysis_progress`, `research_gaps`, `statistics`, `users`, `export_history`
- Row Level Security (RLS) включен для всех таблиц
- Индексы на часто запрашиваемых полях (user_id, article_id, created_at)

**Backend API:**
- Заменить mock данные на реальные запросы к Supabase
- Использовать Supabase client для CRUD операций
- Обработка ошибок Supabase с информативными сообщениями
- Валидация входных данных
- Логирование операций для отладки

**Frontend интеграция:**
- Удалить mock данные из компонентов
- Добавить состояние загрузки для асинхронных операций
- Обработка ошибок с пользователем (toast notifications)
- Кэширование данных на клиенте для быстрого доступа
- Реактивное обновление UI при изменении данных

**Аутентификация:**
- Использовать Supabase Auth для входа/регистрации
- Хранение session token в localStorage
- Автоматическое обновление токена
- Защищенные маршруты требуют аутентификации

### Детальная реализация по модулям:

#### api/routes/articles.ts
```typescript
// GET /articles - получить все статьи пользователя
const { data, error } = await supabase
  .from('articles')
  .select('*')
  .eq('user_id', userId)
  .order('created_at', { ascending: false })

// POST /articles - сохранить статью
const { data, error } = await supabase
  .from('articles')
  .insert([{ ...articleData, user_id: userId }])
  .select()
```

#### api/routes/analysis.ts
```typescript
// GET /analysis/progress - получить прогресс анализа
const { data } = await supabase
  .from('analysis_progress')
  .select('*')
  .eq('article_id', articleId)
  .single()

// GET /analysis/entities - получить сущности
const { data } = await supabase
  .from('entities')
  .select('*')
  .eq('article_id', articleId)

// GET /analysis/interactions - получить взаимодействия
const { data } = await supabase
  .from('interactions')
  .select('*')
  .eq('article_id', articleId)
```

#### api/routes/graph.ts
```typescript
// GET /graph/nodes - получить узлы графа
const { data } = await supabase
  .from('entities')
  .select('id, name, type, degree, betweenness')
  .in('article_id', articleIds)

// GET /graph/edges - получить ребра графа
const { data } = await supabase
  .from('interactions')
  .select('*')
  .in('article_id', articleIds)

// GET /graph/centrality/:type - получить метрики центральности
// Запрос с агрегацией через SQL или через код
```

#### api/routes/gaps.ts
```typescript
// GET /gaps - получить research gaps
const { data } = await supabase
  .from('research_gaps')
  .select('*')
  .eq('user_id', userId)

// POST /gaps - создать новый gap
const { data } = await supabase
  .from('research_gaps')
  .insert([{ ...gapData, user_id: userId }])
```

#### api/routes/statistics.ts
```typescript
// GET /statistics/overview - общая статистика
const [articles, entities, interactions] = await Promise.all([
  supabase.from('articles').select('count'),
  supabase.from('entities').select('count'),
  supabase.from('interactions').select('count'),
])

// GET /statistics/entities/distribution - распределение сущностей
const { data } = await supabase
  .from('entities')
  .select('type')
  .eq('user_id', userId)
```

#### api/routes/export.ts
```typescript
// POST /export/generate - сгенерировать файл
// Получить данные из Supabase
// Преобразовать в формат (JSON, CSV, XLSX, GEXF)
// Вернуть файл для скачивания

const { data: articles } = await supabase
  .from('articles')
  .select('*')
  .eq('user_id', userId)
```

### Критерии завершения

**Обязательные:**
- ✅ Все backend маршруты используют Supabase вместо mock данных
- ✅ CRUD операции для всех сущностей работают корректно
- ✅ Данные сохраняются и восстанавливаются из базы
- ✅ Аутентификация пользователей работает (вход, регистрация)
- ✅ RLS политики обеспечивают безопасность данных между пользователями
- ✅ API возвращает корректные коды ошибок при проблемах
- ✅ Фронтенд корректно обрабатывает асинхронные операции
- ✅ Все страницы отображают реальные данные из базы

**Желательные:**
- 📊 Кэширование результатов для улучшения производительности
- 📊 Логирование операций в отдельную таблицу
- 📊 Webhook уведомления о завершении анализа
- 📊 Оптимизация запросов с индексами
- 📊 Пагинация для больших наборов данных

### Требования к тестированию

**Unit тесты (backend):**
- Тесты для всех API маршрутов с mocked Supabase client
- Проверка корректности SQL запросов
- Обработка ошибок и граничных случаев

**Integration тесты:**
- Тест полного цикла: загрузка статьи → анализ → сохранение → экспорт
- Проверка связей между таблицами (foreign keys)
- Тестирование RLS политик (доступ только к своим данным)

**E2E тесты:**
- Тест загрузки PDF файла через UI
- Проверка сохранения в базу данных
- Тест визуализации графа с реальными данными
- Проверка экспорта в разных форматах

**Ручное тестирование:**
- Загрузка 10+ статей и проверка производительности
- Работа с большими графами (500+ узлов)
- Проверка экспорта больших наборов данных
- Тестирование одновременной работы нескольких пользователей

## 4. Приоритет и сроки

**Приоритет:** HIGH
**Оценка времени:** 3-4 дня
**Срок завершения:** 2026-01-10

**Разбивка по дням:**

День 1: Настройка Supabase и базовая интеграция
- Настройка переменных окружения
- Создание необходимых таблиц (если не созданы)
- Настройка RLS политик
- Тестирование подключения к базе

День 2: Backend API интеграция
- Замена mock данных в api/routes/articles.ts
- Замена mock данных в api/routes/analysis.ts
- Замена mock данных в api/routes/graph.ts
- Тестирование CRUD операций

День 3: Оставшиеся маршруты и аутентификация
- Замена mock данных в api/routes/gaps.ts
- Замена mock данных в api/routes/statistics.ts
- Замена mock данных в api/routes/export.ts
- Реализация аутентификации через Supabase Auth

День 4: Frontend интеграция и тестирование
- Обновление компонентов для работы с API
- Добавление обработки ошибок и загрузки
- Unit тесты для API маршрутов
- Интеграционные тесты
- Документация

## 5. Связанные задачи и документация

**Предыдущие задачи:**
- ✅ Создание структуры проекта (React + Express + TypeScript)
- ✅ Создание всех 7 страниц приложения
- ✅ Создание backend API маршрутов
- ✅ Установка зависимостей

**Связанная документация:**
- `supabase/client.ts` - определения типов таблиц
- `.env.example` - шаблон переменных окружения
- `src/pages/` - компоненты для интеграции

**Последующие задачи:**
- Оптимизация производительности запросов
- Добавление кэширования Redis
- Реализация WebSocket для real-time обновлений
- Добавление CI/CD пайплайнов
- Разработка мобильной версии

## 6. Дополнительные заметки

**Ограничения Supabase:**
- Бесплатный план: 500MB хранилища, 2GB bandwidth
- 100,000 API запросов в день на бесплатном плане
- 1GB базы данных на бесплатном плане

**Рекомендации:**
- Использовать batch операции для множественных вставок
- Добавить индексы на часто запрашиваемые поля
- Реализовать connection pooling для production
- Мониторить использование API лимитов
- Регулярно делать бэкапы данных

**Риски:**
- Может потребоваться миграция данных для больших PDF файлов
- Сложные SQL запросы для метрик центральности
- Таймауты при загрузке больших файлов
- Конфликты при одновременном редактировании

**Митигация рисков:**
- Асинхронная обработка больших файлов с прогресс-баром
- Оптимизация SQL запросов с индексами
- Retry механизм для временных ошибок API
- Версионирование данных для отслеживания изменений
