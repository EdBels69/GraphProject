# Инструкция по развертыванию проекта Graph Analyser на Vercel

## Предварительные требования

1. **Аккаунт Vercel** - зарегистрируйтесь на [vercel.com](https://vercel.com)
2. **Git репозиторий** - проект должен быть загружен в GitHub/GitLab/Bitbucket
3. **Установленный Node.js** - версии 18 или выше

## Способ 1: Развертывание через Vercel CLI (рекомендуется)

### Шаг 1: Установка Vercel CLI

Откройте терминал и выполните:

```bash
npm install -g vercel
```

### Шаг 2: Авторизация в Vercel

```bash
vercel login
```

Следуйте инструкциям в браузере для авторизации.

### Шаг 3: Развертывание проекта

```bash
cd "d:\Проекты\Проект 1\graph analyser"
vercel
```

### Шаг 4: Ответьте на вопросы

1. **Set up and deploy?** - Yes
2. **Which scope do you want to deploy to?** - выберите ваш аккаунт
3. **Link to existing project?** - No
4. **What's your project's name?** - graph-analyser (или другое имя)
5. **In which directory is your code located?** - ./
6. **Want to override the settings?** - No

### Шаг 5: Дождитесь завершения развертывания

Vercel автоматически:
- Установит зависимости
- Соберет проект
- Развернет на CDN
- Предоставит URL (например: https://graph-analyser.vercel.app)

## Способ 2: Развертывание через Vercel Dashboard

### Шаг 1: Загрузка проекта в Git

```bash
cd "d:\Проекты\Проект 1\graph analyser"

# Инициализация Git репозитория (если еще не инициализирован)
git init

# Добавление файлов
git add .

# Первый коммит
git commit -m "Initial commit"

# Создание репозитория на GitHub и загрузка
git branch -M main
git remote add origin https://github.com/YOUR_USERNAME/graph-analyser.git
git push -u origin main
```

### Шаг 2: Импорт в Vercel

1. Перейдите на [vercel.com/new](https://vercel.com/new)
2. Нажмите **"Import Project"**
3. Выберите ваш Git репозиторий (GitHub/GitLab/Bitbucket)
4. Vercel автоматически обнаружит конфигурацию проекта

### Шаг 3: Настройки проекта

**Framework Preset:** Vite
**Root Directory:** ./
**Build Command:** `npm run build`
**Output Directory:** dist
**Install Command:** `npm install`

### Шаг 4: Развертывание

Нажмите кнопку **"Deploy"** и дождитесь завершения.

## Способ 3: Развертывание только Frontend (без Backend)

Если вы хотите развернуть только frontend часть:

### Шаг 1: Создайте `vercel.json`

Файл уже создан в корне проекта:

```json
{
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "http://localhost:3001/:path*"
    },
    {
      "source": "/(.*)",
      "destination": "/$1"
    }
  ]
}
```

### Шаг 2: Развертывание

Используйте любой из способов выше (CLI или Dashboard).

## Устранение проблем

### Проблема: "Command \"npm install\" exited with 1"

**Причина:** Конфликт зависимостей eslint

**Решение:**
```bash
npm install --legacy-peer-deps
```

### Проблема: Ошибки TypeScript при сборке

**Решение:**
```bash
npm run typecheck
```

Исправьте найденные ошибки перед развертыванием.

### Проблема: Backend не работает на Vercel

**Причина:** Vercel предназначен для serverless функций, а не для долгоживущих серверов

**Решения:**

1. **Разверните backend отдельно:**
   - На Vercel как Serverless Functions
   - На Railway, Render или Heroku для Express.js

2. **Используйте Supabase Backend:**
   - Перенесите всю логику в Supabase Edge Functions
   - Vercel будет только frontend

## Рекомендации для продакшена

### 1. Переменные окружения

Добавьте в Vercel Environment Variables:

```
VITE_SUPABASE_URL=your_supabase_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key
```

### 2. Оптимизация

В `vite.config.ts` уже настроены:
- Минификация
- Code splitting
- Tree shaking

### 3. Мониторинг

Настройте:
- Vercel Analytics
- Sentry для error tracking
- Логирование запросов

## После развертывания

1. **Проверьте URL** - откройте предоставленный Vercel URL
2. **Протестируйте функционал:**
   - Главная страница должна загружаться
   - Граф статей должен отображаться
   - Фильтрация должна работать
   - Нажатие на узлы должно показывать детали

3. **Настройте домен (опционально):**
   - Перейдите в Settings → Domains
   - Добавьте ваш домен

## Обновление проекта

После изменений в коде:

```bash
git add .
git commit -m "Update feature"
git push
```

Vercel автоматически развернет новую версию.

## Дополнительные ресурсы

- [Vercel Documentation](https://vercel.com/docs)
- [Vite Deployment Guide](https://vitejs.dev/guide/build.html)
- [React Deployment Guide](https://react.dev/learn/start-a-new-react-project)

---

**Удачи с развертыванием!** 🚀